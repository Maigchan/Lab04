<!DOCTYPE html>
<html>
    <head>
        <title>Clientes</title>
    </head>
    <body>
        <h1>Clientes</h1>
        <ul>
            <% clientes.forEach((cliente) => { %>
            <li><%= cliente.nombre %></li>
            <% }); %>
        </ul>
    </body>
</html>
